import styled from 'styled-components';

const CardInfo = () => {
  return (
    <Container>
      CardInfo
    </Container>
  );
}

const Container = styled.div`

`;

export default CardInfo;
