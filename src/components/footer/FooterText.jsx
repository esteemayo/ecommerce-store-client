'use client';

import styled from 'styled-components';

const FooterText = () => {
  return (
    <div>FooterText</div>
  );
}

const Text = styled.p`
  
`;

export default FooterText;
