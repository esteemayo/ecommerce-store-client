'use client';

import styled from 'styled-components';

const FooterLink = () => {
  return (
    <div>FooterLink</div>
  );
}

const LinkContainer = styled.ul`
  
`;

export default FooterLink;
