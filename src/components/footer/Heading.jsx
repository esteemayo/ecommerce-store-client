'use client';

import styled from 'styled-components';

const Heading = ({ title }) => {
  return (
    <Header>{title}</Header>
  );
}

const Header = styled.h4`
  
`;

export default Heading;
