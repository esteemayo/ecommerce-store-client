'use client';

import styled from 'styled-components';

const CartCounter = () => {
  return (
    <div>CartCounter</div>
  );
}

const Container = styled.div`

`;

export default CartCounter;
