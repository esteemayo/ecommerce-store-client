'use client';

import styled from 'styled-components';

const CartInfo = () => {
  return (
    <div>CartInfo</div>
  );
}

const Container = styled.div`
  
`;

export default CartInfo;
