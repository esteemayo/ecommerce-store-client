import styled from 'styled-components';

const CartHeader = () => {
  return (
    <Container>
      CartHeader
    </Container>
  );
}

const Container = styled.div`
  
`;

export default CartHeader;
