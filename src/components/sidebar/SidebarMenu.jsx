'use client';

import styled from 'styled-components';

const SidebarMenu = () => {
  return (
    <div>SidebarMenu</div>
  );
}

const Container = styled.div`
  
`;

export default SidebarMenu;
