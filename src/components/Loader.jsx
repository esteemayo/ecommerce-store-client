import styled from 'styled-components';

const Loader = () => {
  return (
    <Container>
      Loader
    </Container>
  );
}

const Container = styled.div`
  
`;

export default Loader;
