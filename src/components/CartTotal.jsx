import styled from 'styled-components';

const CartTotal = () => {
  return (
    <Container>
      CartTotal
    </Container>
  );
}

const Container = styled.div`
  
`;

export default CartTotal;
