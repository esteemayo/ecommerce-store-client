import styled from 'styled-components';

const Product = () => {
  return (
    <Container>
      Product
    </Container>
  );
}

const Container = styled.article`
  width: 25%;
  display: flex;
  align-items: center;
  gap: 5rem;
`;

export default Product;
