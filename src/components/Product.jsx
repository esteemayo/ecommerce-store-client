import styled from 'styled-components';

const Product = () => {
  return (
    <Container>
      Product
    </Container>
  );
}

const Container = styled.article`
  
`;

export default Product;
