import styled from 'styled-components';

const OrderStatus = () => {
  return (
    <Container>
      OrderStatus
    </Container>
  );
}

const Container = styled.section`
  
`;

export default OrderStatus;
