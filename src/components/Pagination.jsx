import styled from 'styled-components';

const Pagination = () => {
  return (
    <Container>
      Pagination
    </Container>
  );
}

const Container = styled.section`
  
`;

export default Pagination;
