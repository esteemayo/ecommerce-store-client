'use client';

import styled from 'styled-components';

const EmptyState = () => {
  return (
    <div>EmptyState</div>
  );
}

const Container = styled.main`
  
`;

export default EmptyState;
