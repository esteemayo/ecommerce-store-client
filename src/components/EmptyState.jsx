'use client';

import styled from 'styled-components';

const EmptyState = () => {
  return (
    <Container>
      EmptyState
    </Container>
  );
}

const Container = styled.main`
  
`;

export default EmptyState;
