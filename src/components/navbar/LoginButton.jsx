'use client';

import styled from 'styled-components';

const LoginButton = () => {
  return (
    <>LoginButton</>
  );
}

const Button = styled.button`
  
`;

export default LoginButton;
