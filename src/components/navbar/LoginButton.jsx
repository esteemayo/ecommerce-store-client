'use client';

import styled from 'styled-components';

const LoginButton = () => {
  return (
    <div>LoginButton</div>
  );
}

const Button = styled.button`
  
`;

export default LoginButton;
