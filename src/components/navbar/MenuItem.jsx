'use client';

import styled from 'styled-components';

const MenuItem = () => {
  return (
    <div>MenuItem</div>
  );
};

const Container = styled.li`
  
`;

export default MenuItem;
