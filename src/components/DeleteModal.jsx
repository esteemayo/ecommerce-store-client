import styled from 'styled-components';

const DeleteModal = () => {
  return (
    <Container>
      DeleteModal
    </Container>
  );
}

const Container = styled.section`
  
`;

export default DeleteModal;
