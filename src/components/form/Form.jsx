import styled from 'styled-components';
import PropTypes from 'prop-types';

const Form = () => {
  return (
    <div>Form</div>
  );
}

const StyledForm = styled.form`
  
`;

export default Form;
