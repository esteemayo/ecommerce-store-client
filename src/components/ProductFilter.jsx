import styled from 'styled-components';

const ProductFilter = () => {
  return (
    <Container>
      ProductFilter
    </Container>
  );
}

const Container = styled.section`
  padding: 7rem 0;
  width: 100%;
  max-width: 102.4rem;
  margin: 0 auto;
`;

export default ProductFilter;
