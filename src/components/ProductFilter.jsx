import styled from 'styled-components';

const ProductFilter = () => {
  return (
    <Container>
      ProductFilter
    </Container>
  );
}

const Container = styled.section`
  
`;

export default ProductFilter;
