import styled from 'styled-components';

const ProductCard = () => {
  return (
    <Container>
      ProductCard
    </Container>
  );
}

const Container = styled.article`
  
`;

export default ProductCard;
