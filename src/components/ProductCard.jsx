import styled from 'styled-components';

const ProductCard = () => {
  return (
    <Container>
      ProductCard
    </Container>
  );
}

const Container = styled.div`
  
`;

export default ProductCard;
