import styled from 'styled-components';

const Category = () => {
  return (
    <Container>
      Category
    </Container>
  );
}

const Container = styled.article`
  
`;

export default Category;
