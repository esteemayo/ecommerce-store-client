import styled from 'styled-components';

const PaymentModal = () => {
  return (
    <Overlay>
      PaymentModal
    </Overlay>
  );
}

const Overlay = styled.aside`
  
`;

export default PaymentModal;
