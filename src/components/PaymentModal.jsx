import styled from 'styled-components';

const PaymentModal = () => {
  return (
    <Overlay>
      PaymentModal
    </Overlay>
  );
}

const Overlay = styled.aside`
  width: 100vw;
  height: 100%;
`;

export default PaymentModal;
