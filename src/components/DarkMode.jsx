import styled from 'styled-components';

const Darkmode = () => {
  return (
    <Container>
      Darkmode
    </Container>
  );
}

const Container = styled.div`
  
`;

export default Darkmode;
