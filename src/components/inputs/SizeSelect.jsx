'use client';

import styled from 'styled-components';

const SizeSelect = () => {
  return (
    <div>SizeSelect</div>
  );
}

const Container = styled.div`
  
`;

export default SizeSelect;
