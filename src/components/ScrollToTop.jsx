import styled from 'styled-components';

const ScrollToTop = () => {
  return (
    <Container>
      ScrollToTop
    </Container>
  );
}

const Container = styled.div`
  
`;

export default ScrollToTop;
