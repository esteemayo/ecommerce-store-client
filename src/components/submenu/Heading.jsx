'use client';

import styled from 'styled-components';

const Heading = ({ title }) => {
  return (
    <div>Heading</div>
  );
}

const Header = styled.h4`
  
`;

export default Heading;
