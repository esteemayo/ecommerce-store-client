'use client';

import styled from 'styled-components';

const ProductInfo = () => {
  return (
    <Container>ProductInfo</Container>
  );
}

const Container = styled.div`
  
`;

const Heading = styled.h4`
  
`;

export default ProductInfo;
