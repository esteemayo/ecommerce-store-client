'use client';

import styled from 'styled-components';

const ProductInfo = () => {
  return (
    <div>ProductInfo</div>
  );
}

const Container = styled.div`
  
`;

export default ProductInfo;
