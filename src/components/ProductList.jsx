import styled from 'styled-components';

const ProductList = () => {
  return (
    <Container>
      ProductList
    </Container>
  );
}

const Container = styled.section`
  
`;

export default ProductList;
