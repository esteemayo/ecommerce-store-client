'use client';
import styled from 'styled-components';

const Account = () => {
  return (
    <Container>
      Account
    </Container>
  );
}

const Container = styled.main`
  
`;

export default Account;
