import styled from 'styled-components';

const Products = () => {
  return (
    <Container>
      Products
    </Container>
  );
}

const Container = styled.main`
  
`;

export default Products;
