'use client';
import styled from 'styled-components';

const Register = () => {
  return (
    <div>Register</div>
  );
}

const Container = styled.main`
  
`;

export default Register;
