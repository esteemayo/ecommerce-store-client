import styled from 'styled-components';

const Order = () => {
  return (
    <Container>
      Order
    </Container>
  );
}

const Container = styled.main`
  
`;

export default Order;
