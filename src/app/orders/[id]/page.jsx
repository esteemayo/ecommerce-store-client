'use client';
import styled from 'styled-components';

const Order = () => {
  return (
    <Container>
      Order
    </Container>
  );
}

const Container = styled.main`
  width: 100vw;
  padding: 8rem 0;
`;

export default Order;
