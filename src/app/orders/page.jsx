'use client';
import styled from 'styled-components';

const Orders = () => {
  return (
    <Container>
      Orders
    </Container>
  );
}

const Container = styled.main`
  
`;

export default Orders;
