'use client';
import styled from 'styled-components';

const page = () => {
  return (
    <Container>
      page
    </Container>
  );
}

const Container = styled.main`
  width: 100vw;
  height: 100vh;
`;

export default page;
