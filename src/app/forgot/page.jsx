'use client';
import styled from 'styled-components';

const page = () => {
  return (
    <Container>
      page
    </Container>
  );
}

const Container = styled.main`
  
`;

export default page;
