import styled from 'styled-components';

const Success = () => {
  return (
    <Container>
      Success
    </Container>
  );
}

const Container = styled.main`
  
`;

export default Success;
