'use client';
import styled from 'styled-components';
import Slider from '@/components/Slider';

const Home = () => {
  return (
    <Container>
      <Slider />
    </Container>
  );
}

const Container = styled.div``;

export default Home;
